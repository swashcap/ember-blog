<div class="posts">
  <h1>Posts!</h1>
  <ul class="list-unstyled">
    {{#each}}
      <li>
        {{#link-to 'post' this}}
          {{title}}
          <span class="text-muted">by {{author.name}}</span>
        {{/link-to}}
      </li>
    {{/each}}
  </ul>
</div>
